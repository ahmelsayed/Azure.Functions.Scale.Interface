import * as React from 'react';
import { Route } from 'react-router-dom';
import ScaleInterface from './components/ScaleInterface';

export const routes = <Route exact path='/' component={ScaleInterface} />
